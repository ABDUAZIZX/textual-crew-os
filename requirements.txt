# Runtime dependencies for Textual Crew OS.
# Source of truth is pyproject.toml; this mirrors the runtime deps for
# tooling that expects a requirements.txt. Dev/test extras live under
# pyproject's [project.optional-dependencies] dev group:
#     pip install -e ".[dev]"
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
anthropic>=0.43
fastapi>=0.111
# starlette is transitive via fastapi; pin >=1.0.1 to clear PYSEC-2026-161.
starlette>=1.0.1
uvicorn[standard]>=0.30
websockets>=12.0
jinja2>=3.1
structlog>=24.1
psutil>=5.9
nvidia-ml-py>=12.555
aiosqlite>=0.20
click>=8.1
rich>=13.7
python-dotenv>=1.0
